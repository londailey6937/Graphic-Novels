# generated: every artifact here is rebuilt by tools/ in one command
build/

# macOS
.DS_Store

# python
__pycache__/
*.pyc
venv/

# the editor keeps a plate it replaces; local undo, not history
images/.replaced/

# API keys — never commit these. This repo is public.
.env
.bfl-key
